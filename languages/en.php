<?php
$english = array(
	'lazy_load:label:custom_selectors' => "Custom jquery selectors",
	'lazy_load:help:custom_selectors' => "Use this to specifically target lazy_load to problem areas.  Usually problem areas include columns where one column is below another in the DOM, but beside it in the display.  Also for elements that use internal scroll bars."
);
					
add_translation("en", $english);